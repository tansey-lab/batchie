process {

    publishDir = { "${params.outdir}" }

    withName: ADVANCE_RETROSPECTIVE_EXPERIMENT {
        ext.args = [
            '--model', 'SparseDrugCombo',
            '--model-param', 'n_embedding_dimensions=2'
        ].join(' ')
    }
}
