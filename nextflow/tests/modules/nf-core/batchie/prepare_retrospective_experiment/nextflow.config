process {

    publishDir = { "${params.outdir}" }

    withName: PREPARE_RETROSPECTIVE_EXPERIMENT {
        ext.args = [
            '--plate-generator', 'RandomPlateGenerator'
        ].join(' ')
    }
}
